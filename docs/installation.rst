============
Installation
============

At the command line::

    $ easy_install clipmanager

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv clipmanager
    $ pip install clipmanager